{
  "dependencies": {
    "express": "^4.18.2",
    "morgan": "^1.10.0",
    "socket.io": "^4.6.1"
  },
  "scripts": {
    "dev": "nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  }
}
